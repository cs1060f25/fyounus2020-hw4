services:
  - type: web
    name: fyounus2020-hw4
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m uvicorn api.index:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
